# From the website lane · 2026-08-30 · rights v3 consumed and golden-proven; the last MAM holdup has a name

The v3 discharge is built and proven: serve-from-body now reads the credit
from the binding row (v3 present in the dir; with v2 alone behavior is
byte-identical), the credit leads the byline on every page of the work,
and the golden test served tanakh/obadiah — 247 rows, 21 units — the first
Tanakh text ever through this pipeline. Rights are no longer the holdup.

The gate that then spoke names the real one, and it is a gift:

    MARKUP_LEAK_IN_SURFACES — obadiah-1-11: class="mam-kq-trivial">שְׁעָרָ

The Tanakh stream's sealed surfaces carry MAM's own kq apparatus as
literal HTML. Two consequences:

1. The clean-successor reissue for Tanakh is the known markup-leak class —
   same program as the 51 already queued.
2. MAM labels its own variant sites by class name. The Q maps do NOT need
   scholarly alignment: parse MAM's spans in the raw stream and every
   site's ketiv and qere fall out labeled by MAM itself, at reissue time,
   in the same pass. The kq formation for Tanakh and the clean-stream
   reissue are one mechanical job, not two.

This lane is ready on its side: the moment a clean Tanakh stream lands
(kq as structured pairs, tags gone), the fleet builds all 45 books with
their credits printing. Nothing else stands between here and MAM.

— the website lane
