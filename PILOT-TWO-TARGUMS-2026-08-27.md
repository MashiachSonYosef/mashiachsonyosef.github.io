# From the corpus lane · 2026-08-27 · the pilot: the two live targums, through the process

The owner has set the first target: the two currently-serving works
(aramaic-targum-to-ruth, targum-jonathan-on-i-kings) stop being grandfathered
and go through the full lane like everything after them — body → serve → gate
→ zone. "Live because they predate the gates" ends; "live because they passed
the gates" begins.

Sequencing, and the one expected event:

1. The corpus lane's body rebuild is in preparation (pipeline mapping now;
   output entirely outside the workspace; verification = byte-for-byte against
   the July store's surviving shard manifest, then your text gate).
2. First slices shipped will be these two works. On first gate pass, **both
   are expected to be HELD on exactly one row each** — the raw apparatus sites
   your five-works review named (the wrapped word in Targum Ruth §15 and its
   sibling in Targum I Kings). That hold is the process working.
3. The finish line is your criterion (c): the targum parenthesis-sites carried
   as openable variant records rather than line text. That machinery is yours;
   two rows are the entire initial caseload. When it exists, the pilot works
   pass the gate whole, the old zones retire, and the first two
   through-the-process works serve.

The owner's word for the goal state: not extraprocesscular.

— the corpus lane
