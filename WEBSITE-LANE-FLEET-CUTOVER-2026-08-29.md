# From the website lane · 2026-08-29 · THE CUT IS MADE — fireandhail.com serves the fleet

The announced cutover executed at 2026-08-29, per the standing announcement
(WEBSITE-LANE-CUTOVER-ANNOUNCEMENT-2026-08-29) and FRAME v1.21
announce-don't-ask. No objection stood on the channel at push time.

## What moved

- `gh-pages`: `569adb4e` → `21234e9f` (one forced update, leased against the
  expected head, then one fast-forward for the census fix).
- Rollback remains one command:
  `git push --force-with-lease origin 569adb4e:gh-pages`.

## What the site now is

- **2,201 works on the shelf** (2,202 zone bins incl. the capability
  fixture), every one through the same pipeline: TEXT → RIGHTS → SERVE →
  ZONE, rights joined from the canonical resolution
  (active-rights-resolution-v2), fail-closed at every gate. No curated
  tier, no POCs, no hand-picked subsets — the gates were the only judges.
- The unified front door: every zone is a book; three-counts receipt pins
  the exact set on disk; two hand-typed targums long gone — the two targums
  on the shelf are pipeline rebuilds under plan titles.
- The owner's ruling of 2026-08-28 is recorded and enacted: the 231
  CC-BY display-ALLOW works serve now on the resolution's display axis;
  their credits attach when the attribution successor is wired.

## The ledger (what is withheld, and why — the gates' own words)

877 chapter-numbering gaps · 599 unit-id shapes unparsed · 93 raw
variant-sites awaiting kq review · 87 attribution-held (whole Tanakh
included — serve refuses display without custody of the credit) · 74
rights-unresolved · 61 text holds · 50 markup leaks withdrawn at the build
gate · 1 filename over the filesystem's limit. Every hold is a typed
refusal in the fleet ledger, re-runnable.

## The suite at the cut

130 check suites over the shape panel (first/last/largest/smallest/flat/
nested/titled/untitled/hebrew-id/latin-id): **all green except
check-w-grain-v1**, which fails by design until the corpus lane ships
sealed W lists per section — its own doctrine says what ends it. Final
rounds fixed at their laws: the reading card's placement budget, the
record's floor and half-the-card maximum, the door's title law restated at
the door's real grain (the .fw word multiset — 6,045 words, every one
owed by data), and the unlanded census reading Hebrew paths raw.

## The corpus lane's convoy — received

- **Attribution v3 acknowledged.** The owner's direct install on gh-pages
  (318 works, candidate-only, current_effect NONE, root cards only) rides
  through the cut byte-exact; its table counter-verified against the
  channel seal (`34dc9de9…`, byte-for-byte) and its own verifier passes
  here (PASS_WORK_ATTRIBUTION_DISPLAY_V3__318_WORKS__ROOT_CARD_ONLY).
  Wiring the root-card display is this lane's next act, after independent
  counter-verification of the full v3 seal set — construction's v1 hold
  does not attach, per the convoy note, and nothing reads the table yet.
- The clean-works queue census is noted; gate replay before any of the 218
  ships, as the census itself demands.

## Asked of the owner

The proxy blinds this lane to the live site. Please eyeball
fireandhail.com — the door, one Hebrew-titled book, one Latin-titled book,
and a word card — and say what you see.

— the website lane
