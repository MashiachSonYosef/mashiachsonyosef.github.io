# From the website lane · 2026-08-27 · what stands on staging, and what the site can expect to serve off it

Written at the owner's word so every lane reads the same picture — Bezalel
in particular, whose custody the counts describe. Sources: the corpus
lane's SWEEP1-REPORT-2026-08-26 and CORRECTION-CUSTODY-NUMBERS-2026-08-26,
this lane's two counter-verifications (855/855 and 1,646/1,646, both sides
byte-exact), and the owner's pilot ruling as posted in
PILOT-TWO-TARGUMS-2026-08-27.

## The 151 that boarded

Everything on `corpus-staging` (tip 165e720) that passed its own seal:
sweep 1's 127 packages plus the earlier shipments (MAM bundle, semantic
candidates, antijoin, batch 3, predecessor stack). Three kinds of thing,
none of them body text:

- **raw material, per source** — the canonical-c0-candidate family, one
  bale per provider (opensubtitles waves, ETCBC DSS and Peshitta,
  Princeton Geniza footnotes, Samaritan Pentateuch, ebible, Pleias, IIP,
  OpenSiddur, and the rest);
- **machinery** — the MAM promotion executors, the version-neutral
  terminal readers, the exact-K handoffs;
- **contracts** — including the cutover-request package that defines the
  atomic cutover equation.

The loom, the thread, and the instructions. No cloth: the handoff index
attests SERVE_SHAPED_CARGO_ABSENT and canonical shards on disk are 0.

## The 36 that were refused

Each fails its own seal — sealed, then its tree was touched afterward, so
the pins no longer match the disk (supersession: materializers v2–v5 all
stale, none sealing clean; two Genesis clean-successors both stale). The
text inside is probably fine; the testimony is not, and mismatched
testimony does not board. They need resealing or retiring on the
construction side. Largest: ia-script-language-ready (702MB, 24 bad
pins), ia-christian-witness (468MB, 24), front-door-three-count-
integration (234MB, 19), digital-syriac (195MB, 24), epidat (72MB, 25).
The refusal is what makes the 151's boarding mean something.

## What the site serves off it

- **Off the 151 as they stand: zero books.** Nothing in them is
  serve-shaped.
- **First through: five** — Genesis, I Kings, Ruth, and the two targums,
  by the owner's pilot ruling: the original proof-of-concept redone
  correctly, expected to arrive with five first-pass holds (one per
  defect class) that are milestones, not faults.
- **Ceiling after: 3,986 works** — the July store's count, carried in
  the head pointer as a memory of what the rebuild restores toward. Not
  a promise: each work arrives only as its slices materialize, and each
  faces the text gate and the licence gate (IIP CC-BY-NC-4.0 displays
  lawfully with the commercial axis blocked; the undeclared is refused
  the way the 36 were). Five first, then a climb toward roughly four
  thousand, with the gates deciding the exact count.

— the website lane
