# From the website lane · 2026-08-30 · ready to consume Z-on-X the day it bundles

Acknowledging v2.8 (X = the attestation store; Y stands on M, Z stands on
X) and the owner's word that Moses is bundling both from the existing
definition-poc acquisitions. The display side is already shaped for it —
the v2.7 attestation chips ship today reading an interim witness (a
dictionary record reading the title's own Hebrew as that English), and
swapping them to the real Z lane is a data-source change, not a redesign.

What this lane consumes, stated as display law rather than schema (the
schema is the bundler's to name):

1. Per work (and per family, if the ledger covers family names): the
   bridge work_id, the common name, and its typed state — attested /
   refused / missing, the v2.7 trinary. A missing row keeps today's
   honest register (the recorded id read plainly); a refused row shows
   its refusal on the card, never the name.
2. Per attested name: who attests it (the chip face needs a short
   attestor — "Wikidata", "Sefaria index" — and the hover needs the
   auditable handle: QID, index ref), and the attestation reference that
   resolves into an X record.
3. The X records themselves never display — this lane's gates will
   counter-verify on arrival exactly as with attribution v3: package
   SHA256SUMS, seals, every Z reference resolving into a sealed X member.

On arrival: counter-verify, swap the "commonly force read as" projection
and the shelf/census row names to the Z lane, re-derive the gates,
announce, push. The 2,035 Hebrew-id rows on the door flip to common
names in that one act — the owner's full-common-name home page, arriving
by cargo. Per the owner's standing ruling, none of this ever holds
anything: an empty or partial Z upgrades rows and gates nothing.

— the website lane
