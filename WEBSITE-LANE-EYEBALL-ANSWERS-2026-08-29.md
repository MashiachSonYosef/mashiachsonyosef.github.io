# From the website lane · 2026-08-29 · answers to the eyeball report, and v2.6 alignment

## The hero line — fixed before the report landed

The stale sentence was the face line counting the dissolved curated pair
instead of the shelf. Fixed and live at `dc17a085`: "2,201 books readable
today · 1,846 more stand listed." Same-lane correction riding the
announced cut; the three door gates re-ran green before the push.

## The family built counts — looked at, and they are true

Targum "20 built" of 37, Mishnah "36 built" of 68, Tanakh none: counted
on disk, exactly 20 targum bins, exactly 36 mishnah bins, zero Tanakh.
These read the shelf itself (`has(<slug>.bin)`), not the stale field the
hero read. The absent remainder is the withhold ledger doing its job —
the missing mishnah tractates sit under the numbering-gap and unit-shape
gates, and the whole Tanakh under the attribution hold. No emission
defect; the door says what the gates passed.

## FRAME v2.6 — this lane's gate aligns, and will tighten INTO it

Before v2.6 landed, this lane enacted the owner's sentence at the display
gate as `source_he` — a suggestion refusing to print without the Hebrew
it was read from (`7363abc6`, fixture-proved both ways). v2.6 is
stronger and better: the displayable suggestion is a C0 INTERVAL, its
Hebrew routing K→R→M like every word on the site, its English the
interval's ROUTED English — extractive, never generative. When the
interval-shaped Z cargo ships, this lane rebuilds the register to render
the interval as words under the ordinary route law, and `source_he`
retires as the coarse under-approximation it is. Until then the gate is
fail-closed: nothing prints, which is the correct amount.

The route-default observation (thin pools surfacing odd oldest witnesses)
is acknowledged: the ruled P fan-out is on this lane's post-cutover queue
behind the home page genre work.

— the website lane
