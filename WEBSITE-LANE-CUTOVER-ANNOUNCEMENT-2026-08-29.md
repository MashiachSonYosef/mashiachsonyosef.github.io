# From the website lane · 2026-08-29 · CUTOVER ANNOUNCED — per the governance rule: announce, don't ask

Per FRAME v1.21 (irreversible outward pushes: intent posted with exact diff
and rollback, shipped absent an evidence-backed objection).

## Intent

Push `claude/test-ir4366` @ `750d7708` to `gh-pages`. fireandhail.com goes
from 2 served works to **2,347**, in one cut, on the shape-panel suite's
green (running at this writing; the push follows it).

## The diff, whole

- gh-pages today: `569adb4e` — the two targums, the two-tier door.
- After: the full fleet pass 2 output. 2,347 zones (the targums rebuilt
  through the same pipeline, golden-identical text), the unified door (a
  zone on the shelf is a book; the curated tier dissolved), 2,345 new work
  addresses, the counts receipt pinning every bin (6,210,248 rendered
  COMPspan records), the zone store re-pinned (2,348 bins), title-from-C0
  mastheads, the suggested-title register with the drawer ruling, the
  canonical rights vocabulary. Holds standing, all named in the ledger:
  877 numbering-gap + 599 unparsed-shape (coordinate frontier), 87
  attribution fail-closed (successor awaited per construction's hold — V1
  NOT wired), 74 rights-unresolved, 61 text-coverage, 1 span-duplicate.
  The owner's rulings recorded: the 231 credit-desirable works serve per
  the resolution's display axis; credits attach when the cleared successor
  lands.

## Rollback

One command, no history rewritten on anyone else's branch:
`git push --force-with-lease origin 569adb4e:gh-pages` restores today's
site byte-for-byte. The DNS, CNAME, and domain do not move either way.

Objections with evidence to this channel; absent one, the push follows the
suite's green.

— the website lane
