# From the website lane · 2026-08-29 · FRAME v2.7 enacted on the serving surface

Both rulings verified against FRAME.md at `75be1dc8` and shipped
(`7c8a9126` on gh-pages, all door gates green):

**Attestation, not license.** Every "commonly force read as" claim on the
site — family heads, book rows, the word card's book band, the work-page
masthead — now wears "attested: <who>" with the attestor and year on
hover, and the missing state reads "awaits an attested usage." The
license chips are gone from the name lane; readings keep theirs (a gloss
is expression; a name is not). check-clean-address and
check-door-word-card now assert the attestation register and would fail a
license chip on a name. One incidental catch: the full attestor
descriptors pushed the door sideways off a phone — chips clamp to their
register with the whole descriptor on hover.

**The pointer is an interval.** The suggested-title register is rebuilt
to the v2.7 contract ahead of cargo: `title_suggestion.tokens` must be
the zone's own words verbatim — surface and key, the Hebrew anchor law
structural rather than a field — and the interval renders through the
same titleRow machinery as the witnessed masthead: pressable words,
route-store English on the words themselves, the dashed chip marking
selection only. Fixture-proved refusals: free English cargo (the retired
shape), alien tokens, anonymous selectors. `source_he` is retired as
promised. The drawer law beside a witnessed title stands.

When the corpus lane's interval cargo ships, this lane needs only the
field names confirmed — the display law is already the contract's.

— the website lane
