# From the website lane · 2026-08-28 · the full fleet ran; three findings; one ask

## The first full run, whole

Every bridge work through TEXT → RIGHTS → SERVE → ZONE with the canonical
rights resolution joined: 4,047 works, 2,385 zones GREEN_BUILT, and every
hold explained — 878 CHAPTER_NUMBERING_GAP (sealed ordinals that skip),
648 UNIT_ID_UNPARSED (coordinate shapes beyond the two laws now written:
named sections, default-default, sub-lettering), 61 TEXT (the coverage
residual), 74 RIGHTS fail-closed unresolved, 1 SPAN_DUPLICATE_KEY, zero
unexplained, zero infrastructure losses. A corrected full pass is running
now (the three laws below); cutover follows it.

## Finding 1 — for the corpus lane: a duplicate key in the compspan template

`w-to-compspan-template-v6.csv.gz` carries **עמסיה twice**. The reader's
span slice refuses duplicate keys, so targum/targum-of-ii-chronicles holds
at ZONE until the template is reissued (or the duplicate is ruled benign
and the slice law amended by the owner). One work, one key, exact file.

## Finding 2 — the ALLOW_WITH_ATTRIBUTION works cannot serve yet, and why

87 RESOLVED works (the Tanakh shelf among them) carry the profile whose
reader display is conditioned on attribution. This lane holds no
attribution string for any work, so they fail closed at RIGHTS with
RIGHTS_ATTRIBUTION_NOT_IN_CUSTODY: serving them dark would misread the
license as a hold; serving them lit uncredited would violate it. This also
retires every curated-book special case cleanly — Genesis and kin hold at
RIGHTS before any door pin is reached.

I looked at discharging it from the N ledger already in custody: the
b-plus-n files carry source_version_title / source_version_url /
effective_source_license, but for the 87 only 3 rows are complete (84 lack
the url), and the batches conflict on 1,809 keys with a precedence rule
this lane does not hold. Constructing credits from that would be guessing.

## The ask — attribution as a derived current, same grade as the rights table

**A per-work attribution table for the 87** (or for all 3,986, if cheap):
`work_id, credit_line (the licensor/version identity as it should print),
source_url, license_link (only where the record carries one)` — one row
per work, sealed with its own SHA256SUMS, shipped on staging or this
branch. The serve join is ready to read it the day it lands: byline and
[H] entry print the record's own strings, visible_in_hebrew_reader flips
on for the profile, and the 87 join the fleet with their credit riding on
every page and every export.

## Finding 3 — coordinate laws are the growth frontier

878 + 648 works hold on coordinate shape alone: rights resolved, text
verified, refused only because their sealed unit ids speak languages the
zone builder does not yet read. Each new coordinate law (gated, like the
flat-sequence law written tonight) moves a shelf from held to serving with
no corpus-side work at all. The flat-sequence contiguity requirement may
itself be over-strict (ordinals that legitimately skip); recorded as a
follow-up with its gate.

— the website lane
