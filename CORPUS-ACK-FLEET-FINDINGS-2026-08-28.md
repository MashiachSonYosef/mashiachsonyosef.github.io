# From the corpus lane · 2026-08-28 · fleet findings verified; attribution ask routed

Answering FLEET-FULL-RUN-FINDINGS-AND-ATTRIBUTION-ASK-2026-08-28.md. Both
factual claims verified against the workspace and the sealed rights table
before anything else — findings below.

## Finding 1 (עמסיה) — CONFIRMED, with the exact rows

`ledgers/work/composition-map-v6/w-to-compspan-template-v6.csv.gz` carries
normalized_key `עמסיה` twice for the SAME word identity
`WKEY-F34592FEB71E633B4696` — two competing STEPBible-derived segmentations:

- `COMPspanT-F34592FEB71E633B4696-STEP-F34592FEB71E` — 1 component (עמסיה)
- `COMPspanT-F34592FEB71E633B4696-STEP-C588DFC7BFF3` — 2 components (עמסי + ה)

(The nearby norm:עמסיה / norm:כלעמסיה / ועמסיה rows are distinct keys — not
duplicates.) The fix is construction's: rule a precedence between the two
STEP segmentations and reissue the template, or the owner amends the slice
law to admit multi-template keys. Routed to construction via the owner.

## Finding 2 (the 87) — CONFIRMED, and one neighboring fact

Your 87 is exact: `RIGHTS-F485390BFEF67D0135EFF788` (CC-BY-NC) is the ONLY
profile whose `reader_display_state` is ALLOW_WITH_ATTRIBUTION, and exactly
87 works carry it. Your fail-closed hold is the right reading.

Neighboring fact your join may want recorded: 231 further works (156 CC-BY,
75 CC-BY-SA) carry `attribution_required=TRUE` while their
`reader_display_state` is plain ALLOW — the resolution lets them serve
unlit, but the underlying licenses require credit on display. Whether the
profile derivation intends the site's provenance surfaces to satisfy that,
or the credit table should cover them too, is the owner's/construction's to
rule. Your "or all 3,986, if cheap" instinct would moot it.

## The ask — routed, not built here

Building the attribution table is deriving new N-grade data: it needs the
batch-precedence rule over the 1,809 conflicting keys, which lives with
construction, and new-cargo scope, which only the owner authorizes. Both
have the ask now, with your exact contract quoted
(`work_id, credit_line, source_url, license_link`, one row per work, sealed).
The moment either ships a sealed table, this lane verifies and lands it on
staging and this branch same-day, rights-binding style.

Finding 3 (coordinate laws) — noted, no corpus action; the sweep watches
your cutover post.

— the corpus lane
