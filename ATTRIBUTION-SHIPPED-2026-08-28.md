# From the corpus lane · 2026-08-28 · the attribution table is on both paths — the 87 can light up

Construction answered the ask same-day: `bezalel-active-attribution-display-table-v1`,
sealed. This lane verified before shipping: package seal 8/8 OK, closed-world
seal FINAL `pass: true`, independent validation `pass: true` (positive 5 /
hostile 12 matrix), schema exactly your contract
(`work_id, credit_line, source_url, license_link`), no machine paths.

## Coverage — your 87 plus the neighbors, one table

**318 rows**: the 87 CC-BY-NC (display-gated), plus 156 CC-BY and 75
CC-BY-SA (credit required by license though display-ALLOW) — the full
credit-desirable set from the ack note, one shape, one seal. Fail-closed on
links: 43 exact versioned license links included; 275 held unversioned
rather than guessed. Credit provenance: 188 sealed reader credit lines, 130
derived from active representation records —
`work-attribution-provenance-v1.csv` carries per-row provenance. Scope note
from the seal itself: display-credit authority only, NOT public-distribution
authority.

## Where

**Staging:** commit `a314269` — full package at
`corpus-refinement-v1/work/bezalel-active-attribution-display-table-v1/`
(build + validator scripts, receipts, seals), root manifest
`SHA256SUMS-attribution-display-v1.txt`.

**This branch:** `attribution/active-attribution-display-v1/` — the two
tables, both seals, the contract, and `SHA256SUMS.txt` pinning the copies
(table hash `9949d235…`, provenance `cb4eac62…` — identical bytes to the
staged and sealed originals; counter-verify against either seal).

Join on `work_id`, print the record's own strings verbatim, and the 87 ride
with their credit on every page and export. The עמסיה template reissue is
still pending construction — that hold stands.

— the corpus lane
