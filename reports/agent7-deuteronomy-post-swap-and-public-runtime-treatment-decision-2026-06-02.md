# Agent 7 Decision: Deuteronomy Post-Swap State And Separate Public Runtime Treatment

Date: 2026-06-02
Authority: Agent 7 strategy / priority control
Agent 6 docket: `reports/agent6-live-deuteronomy-post-swap-runtime-recheck-2026-06-02.md`
Decision type: public-runtime strategy/control wording correction
Publication boundary: publication remains `blocked_no_render`

## Decision

Agent 7 accepts Agent 6's WARN-ACCEPTED post-swap Deuteronomy static HTTP ruling for control-state use only.

Current Deuteronomy strategy state:

`warn_accepted_live_deuteronomy_static_http_current_hud_old_markers_absent_browser_click_and_source_of_truth_open`

Agent 5 must stop describing live Deuteronomy as still serving old HUD unless a newer probe contradicts the Agent 6 docket.

Current HUD is the priority Deuteronomy public reader surface within existing Agent 6 docketed boundaries.

## Deuteronomy Next Actions

Agent 5 should update stale P0 wording now and prepare a bounded deployment source-of-truth packet for the live Deuteronomy swap.

That packet must identify the exact commit, branch, workflow artifact, or worktree used for live Deuteronomy and include:

- `data/public-hud/deuteronomy/**`
- Deuteronomy page hash
- Reader Workbench JS/CSS hashes
- public-HUD manifest, occurrences, route manifest, and sentinel shard hashes
- live URL and cache-busted URL evidence already docketed by Agent 6
- what must not be accepted

Agent 4 should receive only bounded Deuteronomy live browser-click proof at a safe checkpoint if Agent 5/Agent 8 finds Agent 4 idle/stale or Agent 6 requests it. The Agent 4 packet must not expand to Genesis, `/hud-preview/`, source custody, publication, or broad rollout.

## Genesis Treatment

Decision: `separate_restore_work_deferred_pending_owner_or_agent7_product_decision`.

The current reported Genesis 404s are not accepted as intentional final quarantine. Genesis is a separate public-runtime drift/restore candidate, not a Deuteronomy acceptance dependency. Keep it out of Deuteronomy source-of-truth, browser-click, and old-HUD downgrade packets.

Agent 5 may preserve a separate Genesis public-runtime intake note, but should not spend the next P0 packet on Genesis unless the user, Agent 7, or Agent 6 changes priority.

## /hud-preview Treatment

Decision: `intentional_quarantine_non_public_posture_pending_agent6_live_closure`.

The `/hud-preview/` prototype/source-evidence surface should remain non-public or quarantined unless the user explicitly asks to restore it. Current 404s may be consistent with quarantine direction, but they are not closure by themselves.

Agent 5 should keep `/hud-preview/` separate from Deuteronomy and prepare only a separate post-remediation live proof or exact Pages/deployment blocker if Agent 6 or Agent 7 requests that lane. If `/hud-preview/routes/` remains reachable in any future route, it needs either route quarantine proof or documented intended non-public status.

## Control Correction

Update stale control wording now:

- Deuteronomy is no longer described as live old-HUD for the exact Agent 6-reviewed page and dependency set.
- Deuteronomy is WARN-accepted static HTTP evidence only.
- Browser-click proof remains open.
- Deployment source-of-truth for `data/public-hud/deuteronomy/**` remains open.
- Genesis and `/hud-preview/` remain separate public-runtime drift/treatment lanes.

## What Remains Unaccepted

- broad public/runtime acceptance
- live browser-click behavior
- deployed/CDN/cache closure
- old-HUD fallback/rollback closure
- source/provenance custody
- publication readiness
- publication-path support
- route publication support
- Definition authority
- usage-as-definition authority
- Reader Workbench broad rollout
- product/data gate acceptance
- translation output
- accepted translation text
