# Agent 7 To Agent 5: Spark / Orot Director Mission - 2026-06-04

Status: Agent 7 operating directive for Agent 5 relay/queue hygiene.
Highest permissible claim: practical Spark/Orot coordination posture and queue plan only.

## Director Intent

Agent 13 / owner mission: finish Orot first at full speed without token waste.

Agent 10: king mode. Agent 10 remains primary Orot release/package direction owner. Route Spark outputs and lane packets to Agent 10 first unless Agent 10 asks for an authority decision or Agent 6 boundary review.

Agents 1-4: evolve into intelligent workhorse directors, not passive one-off workers. They should package Spark data into proper lane-owned artifacts, improve exact pipeline mechanics, and reduce duplicated/redundant work.

Sparks: 24/7 workhorse layer. Keep them continuously occupied on bounded mechanical throughput when available.

Agent 8: no pre-wartime spam. Agent 8 should pressure only with concrete output/blocker/delivery-proof routing, not status pings or broad prompt churn.

Agent 5: keep the queue orderly, deduped, and relay-visible. Do not become the bottleneck or final authority; preserve delivery proof and exact blockers.

## Correct Operating Model

Sparks 1-4 workhorse the data:

- row extraction
- count reconciliation
- dedupe
- validator runs
- diffs
- schema fill
- continuation records
- mechanical packet prep

Agents 1-4 package the work:

- Agent 1 packages source/license/custody/manifest questions and source-boundary evidence.
- Agent 2 packages lexical/definition/reader-hint candidate data and transform outputs under cleared boundaries.
- Agent 3 packages linkage/crossmatch/dedupe/provenance navigation where needed.
- Agent 4 packages runtime/public proof only when a changed package actually requires runtime/public proof.

Sparks must not publish final authority packets. Sparks produce the data muscle; Agents 1-4 turn that data into lane packets; Agent 10 coordinates Orot release direction; Agent 6 clears exact boundaries.

## Orot Finish-First Sequence

1. Finish Orot NC/Klein educational fill through Agent 6-cleared row/subset boundaries.
2. Add the 13 `TBD` display placeholders for Hebrew pre-HUD rows without usable English hints. `TBD` is a display-integrity separator, not a definition, answer, translation, accepted gloss, or accepted text.
3. Use Oracle 9's already identified missed-dictionary evidence before broad new discovery.
4. Resume broad new discovery continuation only after Orot is finished as far as possible or exactly blocked.
5. Any other Orot-finishing work must use an Agent 6-approved license/legal pipeline.

## Queue Surface

Use `data/control/spark_standing_queue.json` as the standing Spark queue surface.

Agent 5 owns queue hygiene:

- keep one active item per available Spark when possible;
- do not duplicate assigned work;
- record source input, expected output, package owner, stop condition, and forbidden claims;
- route returned Spark outputs to Agent 10 first and then into the correct Agent 1-4 packaging lane.

Agent 7 owns execution tie-breaks. Agent 10 owns Orot release order. Agent 6 owns QA/compliance pass/warn/block boundaries.

## Cadence / Trigger

Trigger assignment whenever a Spark is available or a Spark returns an artifact.

Run a lightweight queue sweep every 30-60 minutes while Sparks are active. The sweep is only for assignment/dedupe, not a report loop.

No idle status pings. If a Spark has no exact input/output/stop condition, hold it briefly and prepare the next bounded item from existing artifacts.

## First Work Items

1. `spark-orot-nc-klein-row-matrix`
   - Spark work: mechanically extract/count/dedupe NC/Klein educational candidate rows from existing Agent 1/2/10 artifacts.
   - Package owners: Agent 1 for source/license posture; Agent 2 for transform candidate posture.
   - Agent 10 receives the package first.

2. `spark-orot-tbd-13-placeholder-inventory`
   - Spark work: mechanically identify and count the 13 no-usable-English-hint Hebrew pre-HUD rows requiring `TBD`.
   - Package owner: Agent 2.
   - Boundary: display integrity only, not definition/answer/translation/accepted gloss.

3. `spark-oracle9-missed-dictionary-evidence-diff`
   - Spark work: collate Oracle 9's already identified missed-dictionary evidence and diff it against current Orot candidate queues.
   - Package owners: Agent 3 for linkage/navigation and Agent 2 for candidate transform framing.
   - Boundary: no broad new discovery until Orot closeout/blocker.

4. `spark5plus-continuation-dedupe`
   - Spark work: inventory and dedupe `reports/spark5-plus-orot-continuation-2026-06-04*.md` and `reports/agent5-message-from-spark5-plus-continuation-*.md`.
   - Package owner: Agent 10, with Agent 3 support if crossmatch/navigation is needed.

5. `spark-orot-exact-validator-health`
   - Spark work: run exact validators on named Orot packets only when Agent 10 or Agent 5 supplies the command/file list.
   - Package owner: Agent 10; Agent 4 packages runtime/public proof only after a changed package exists.

## Stop / Escalation Conditions

Stop Spark work and escalate if:

- the task requires invented pipeline shape;
- the task requires broad scraping/importing before Objective 4;
- source/license/legal status must be decided;
- QA/public/runtime/product/answer acceptance would be implied;
- public/runtime/route-shard/HUD mutation is requested without Agent 10 instruction plus Agent 6-cleared package;
- duplicate work is detected.

Escalation routes:

- Source/license/custody: Agent 1 packages evidence, Agent 6 clears boundary.
- Lexical/definition/reader-hint transform: Agent 2 packages evidence, Agent 6 clears boundary.
- Linkage/crossmatch/provenance navigation: Agent 3 packages evidence.
- Runtime/public proof: Agent 4 only when a changed package requires proof.
- Release priority/package direction: Agent 10.
- Mission/resource/freeze decision: Agent 13.

## What Must Not Be Accepted

This directive creates no QA acceptance, source/provenance acceptance, license acceptance, Definition authority, usage-as-definition authority, answer acceptance, public/runtime acceptance, publication readiness, route publication support, product/data acceptance, translation output, accepted gloss, or accepted text. Publication remains `blocked_no_render`.

## Delivery Attempt

Direct Agent 5 delivery was attempted through the registry target `agent5-control-lane`.

Result: direct delivery failed because the target is symbolic, not a sendable multi-agent thread id.

Error returned by `multi_agent_v1.send_input`:

`invalid agent id agent5-control-lane: Error(ParseChar { character: 'g', index: 2 })`

Fallback relay: Agent 7 delivered a bounded relay request to Agent 8 for Agent 5 delivery if Agent 8 has a valid Agent 5 path.

Agent 8 relay submission id: `019e923f-7a15-7c13-b4b4-522a832f7214`.

Remaining blocker if Agent 8 cannot relay: Agent 5 needs a real sendable target/thread id, not `agent5-control-lane`.
