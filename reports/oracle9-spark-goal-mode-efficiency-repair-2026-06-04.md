# Oracle 9 Spark Goal-Mode Efficiency Repair - 2026-06-04

## Owner Line

Spark lanes should enter durable goal mode. The efficiency move is not to conserve Spark tokens; it is to unload bounded mechanical work from Agents 1-4, Agent 10, and coordinators onto Sparks while keeping acceptance authority out of Spark scope.

## Current Contradiction

Broad floor is recorded, but several Spark lanes still terminate at `no_queued_item`, `sleep`, `notLoaded`, or replacement-thread ambiguity. That is not enough for the owner target of bringing every work/book toward an Orot-level pipeline.

Evidence:
- `data/control/agent_goal_board.json` records `active_mode: BROAD_CORPUS_EXPANSION`.
- `data/control/spark_standing_queue.json` records Sparks as continuous mechanical workhorses but also records idle/sleep/no-queued-item states.
- Thread roster check showed old Spark-1 `systemError`, Spark-2 `idle`, Spark-3 `notLoaded`, Spark-4 `active`, and old Spark-10 `idle`.
- Replacement Spark-1 thread is `019e9267-c7bc-7af1-93a2-72a381b89bf0`.
- Replacement/current Spark-10 thread is `019e925b-f976-73f2-a859-af586ac3887c`.

## Correct Operating Rule

Sparks should run goal mode when they have exact bounded mechanical work. If a Spark finishes and no queue item exists, the coordinator response is not a status memo; it is:

1. route returned artifact to the package owner;
2. seed the next exact mechanical workset if possible;
3. otherwise record the exact missing input/command/output/schema/validator as the blocker.

## Spark Goal Mode

| Spark | Goal-mode lane | Work to unload | Stop condition |
|---|---|---|---|
| Spark-1 | source/license/custody mechanics | source family counts, license flags, custody matrices, NC/commercial-clean separation, per-book source manifests | compact artifact or exact missing input/command/schema blocker |
| Spark-2 | definition/lemma/reader-hint mechanics | dictionary candidate rows, lemma coverage, reader-hint samples, NC educational candidate flags, unmatched counts | compact artifact or exact missing input/command/schema blocker |
| Spark-3 | linkage/dedupe/navigation mechanics | route-card links, duplicate keys, source-route matrices, candidate-card dedupe, navigation gaps | compact artifact or exact missing input/command/schema blocker |
| Spark-4 | validator/prereq/runtime mechanics | validators, prereq checks, changed-package checks, baseline runtime proof commands | compact artifact or exact no-changed-input blocker |
| Spark-10 | release/package shadow mechanics | release-relevance intake, package matrix checks, Agent 10 mechanical sweeps, handoff readiness checks | compact artifact or exact no-new-release-relevant-output blocker |

## Coordinator Instruction

Agent 13 owns the mode choice and target list. Agent 7 staffs/wakes. Agent 5 reseeds and proves delivery. Agent 8 applies pressure and routes exact blockers. None of them should spend large tokens duplicating Spark mechanics.

Required next proof from Agent 13/7/5:

`spark | thread id | current goal-mode item | package owner | expected artifact | next reseed rule | blocker if no work`

## Boundaries

This is Oracle 9 efficiency advice and delivery pressure only. It does not create QA acceptance, source/provenance acceptance, license acceptance, Definition authority, runtime acceptance, publication readiness, product/data acceptance, answer acceptance, accepted gloss, or accepted text.
