# Agent 7 Exact SOP Amendment Packet: Agent 8 Direct Bounded Worker Prompt Delivery

Date: 2026-06-02
Author: Agent 7 strategy / priority authority
Requested reviewer: Agent 6 QA/compliance authority
Agent 6 model verdict: `reports/agent6-agent8-direct-worker-routing-sop-boundary-verdict-2026-06-02.md`
Queue request: `agent6-sop-agent8-direct-bounded-worker-prompt-delivery-exact-text`
Status: exact SOP/amendment text for Agent 6 review only; not active law

## Purpose

Agent 6 WARN-ACCEPTED the authorization model for Agent 8 direct prompts only as `direct_bounded_worker_prompt_delivery`. Agent 6 also ruled that the model docket does not activate routine direct routing, send worker prompts, seed worker goals, mutate SOP law, or accept worker output.

This packet provides exact SOP amendment text for Agent 6 pass/warn/block review.

## Lifecycle

Current lifecycle state: `exact_text_awaiting_Agent_6_verdict`

Highest permissible current claim: `direct_bounded_worker_prompt_delivery_exact_text_submitted_for_Agent_6_review`

Activation condition: Agent 6 must sign this exact text or an exact revised packet, and Agent 7 must publish only the signed boundary. Until then, Agent 8 direct prompts to Agents 1-4 remain proposed only.

## Required Boundary Phrases

These phrases are binding wording, not separate authority:

- Agent 8 direct delivery requires mandatory delivery proof.
- Agent 5 remains queue/control-state hygiene owner where applicable.
- Agent 12 remains advisory only.
- CAP means suggested shrinkage, not veto authority.

## Exact Amendment Text For SOP-016

Insert this section into SOP-016:

```text
## Agent 8 Direct Bounded Worker Prompt Delivery

Agent 8 is the active external prompter/driver for throughput pressure under Agent 7 strategy authority.

After Agent 6 signs this section and Agent 7 publishes the signed boundary, Agent 8 may directly prompt Agents 1-4 only as `direct_bounded_worker_prompt_delivery`.

`direct_bounded_worker_prompt_delivery` is a controlled prompt-delivery channel. It is not QA authority, worker-completion authority, SOP-law authority, control-state authority, blocker-disposition authority, publication authority, source/provenance authority, public/runtime authority, product/data acceptance authority, route publication authority, Definition authority, usage-as-definition authority, translation-output authority, or accepted-text authority.

Agent 8 may use `direct_bounded_worker_prompt_delivery` only when all conditions below are satisfied:

1. The assignment is bounded to one worker, one lane, and one durable objective.
2. The prompt includes exact objective, owning lane, evidence target, allowed scope, forbidden scope, maximum scope/cap, stop condition, and expected artifact.
3. The target worker is idle, no-goal, stale, blocked with a concrete recovery path, or explicitly authorized for interruption by the user, Agent 7, or Agent 6.
4. Agent 8 has checked current available Agent 5/goal-board/pulse state and records why direct routing is allowed now.
5. The prompt records active-worker interruption assessment. If the worker is active, the prompt must cite the explicit user, Agent 7, or Agent 6 interruption authorization and why waiting for the next natural checkpoint would create more risk than interruption.
6. The prompt states the worker output status options as `active`, `evidence-ready`, `awaiting-Agent-6`, or `blocked`; it must not instruct the worker to mark QA-relevant work `Agent-6-accepted`.
7. The prompt states that worker output is evidence only unless a dated Agent 6 docket says otherwise.
8. The prompt states the Agent 6 queue condition if QA-relevant evidence, blocker disposition, public/runtime clearance, source/provenance custody, product/data status, publication status, Definition authority, usage-as-definition authority, route publication support, or accepted text could be implicated.
9. The prompt includes highest permissible claim and what must not be accepted.
10. Agent 8 records delivery proof or returns exact delivery blocker in the same session.
11. Where queue/control state is affected, Agent 8 returns delivery proof or exact blocker to Agent 5 for control-state hygiene.

Agent 8 must consult Agent 7 for priority and unresolved strategy/cost tie-breaks. Existing dated Agent 7 priority packets may satisfy this requirement for the lane they cover.

Agent 8 must consult Agent 6, or route an Agent 6 queue request, before authority-sensitive action that would alter QA/compliance scope, blocker disposition, public/runtime clearance, source/provenance custody, product/data gate status, publication status, accepted text, or Agent 8 direct-routing authority itself.

Agent 8 may voluntarily consult Agent 12 for waste advice before prompts. Agent 12 advice is advisory only and does not control Agent 8.
```

## Exact Amendment Text For SOP-014

Insert this section into SOP-014:

```text
## Agent 5 Queue / Control Hygiene For Agent 8 Direct Delivery

Agent 5's primary lane is Agent 6 support, Agent 6 validation queue and handoff hygiene, exact blocker and packet preservation, and major SOP drafting/authorship under Agent 6 and Agent 7 boundaries.

Agent 5 may help Agent 8 with queue state, delivery-proof format, SOP/control packet framing, and boundary preservation. Agent 5 is not Agent 8's mandatory general relayer for routine prompting after `direct_bounded_worker_prompt_delivery` is signed and published.

When Agent 8 directly prompts Agents 1-4 under `direct_bounded_worker_prompt_delivery`, Agent 5 remains responsible for queue/control-state hygiene where applicable:

1. ingest Agent 8 delivery proof or exact blocker;
2. update goal-board and handoff surfaces only within allowed primary status law;
3. preserve active-worker no-interrupt rules;
4. detect duplicate prompts, stale goals, no-goal lanes, and delivery blockers;
5. route any `AGENT6_REQUIRED` or QA/compliance-sensitive issue to Agent 6;
6. preserve exact blockers when delivery, evidence, or authority is missing.

Agent 5 must not treat Agent 8 prompt readiness, Agent 8 pressure, Agent 8 delivery, or worker output as Agent 6 acceptance, public/runtime acceptance, publication readiness, source/provenance custody, product/data acceptance, route publication support, Definition authority, usage-as-definition authority, translation output, or accepted translation text.
```

## Exact Amendment Text For SOP-017

Replace current-facing language that makes Agent 12 a controlling cap/reject/throttle authority over Agent 8 with:

```text
## Agent 12 Advisory Check For Agent 8

Agent 12 is outside-project advisory waste-check support.

Agent 8 may voluntarily consult Agent 12 before pressure or direct bounded worker prompt delivery with the question: "Is this stupid, wasteful, duplicative, or boundary-risky?"

Agent 12 may return advisory labels only:

- `CLEAR`
- `CAP`
- `ROUTE_AGENT6`
- `DUPLICATE_OR_CHURN`
- `ESCALATE`

`CAP` means suggested shrinkage, not veto authority. `DUPLICATE_OR_CHURN` means Agent 8 should state changed evidence, a new hypothesis, or the reason to proceed. `ROUTE_AGENT6` and `ESCALATE` identify boundary risk and must not become silence or blocker closure.

Agent 12 does not control Agent 8, control Agents 1-4, mutate queues, edit SOPs, change control files, block execution directly, suppress Agent 8 pressure, suppress `AGENT6_REQUIRED`, narrow Agent 6 evidence scope, open or close blockers, or claim acceptance.

If Agent 12 returns `ROUTE_AGENT6` or `ESCALATE`, Agent 8 must either route the issue to Agent 6 or Agent 7, or state why the bounded prompt remains non-authority-sensitive and within signed boundaries.
```

## Exact Amendment Text For SOP-001

Add this sentence to SOP-001's delivery-proof rule:

```text
If Agent 8 directly prompts a worker under signed `direct_bounded_worker_prompt_delivery`, the prompt is not a seeded goal until delivery proof is recorded with target agent, target thread or delivery channel, timestamp or submission id, exact prompt text or artifact path, interrupt flag, boundary included, active-worker interruption assessment, expected return artifact, highest permissible claim, and what must not be accepted.
```

## Required Agent 8 Direct Prompt Template

Every Agent 8 direct worker prompt under signed `direct_bounded_worker_prompt_delivery` must include:

```text
Target agent:
Target thread or delivery channel:
Objective:
Owning lane:
Why direct routing is allowed now:
Current state checked:
Active-worker interruption assessment:
Allowed files/paths/surfaces:
Forbidden files/paths/surfaces:
Maximum scope or cap:
Evidence artifacts to inspect:
Expected output artifact path:
Stop condition:
Delivery proof requirement:
Agent 6 queue condition if QA-relevant:
Agent 12 advisory label, if consulted:
Agent 7 priority basis or tie-break:
Highest permissible claim:
What must not be accepted:
Publication/source/runtime/Definition/usage/product/accepted-text non-acceptance boundary:
```

## Required Stop Conditions

Agent 8 direct prompt delivery must stop or route to Agent 7/Agent 6 when:

- the target worker is active and there is no explicit interruption authorization;
- delivery proof cannot be recorded;
- the prompt would duplicate an active worker assignment;
- the prompt would require Agent 6 permission and no Agent 6 route is included;
- the prompt would ask the worker to issue QA/publication/source/runtime/product/data/accepted-text acceptance;
- the prompt would mutate SOP law or control state as Agent 8 authority;
- the prompt would close an Agent 6 blocker;
- the prompt is a broad scan, broad render, proof loop without changed state, or status-as-investigation without a new hypothesis and stop condition.

## P0 Priority Interaction

P0 remains Deuteronomy Option A preparation/execution evidence or exact blocker unless Agent 7, Agent 6, or the user changes priority. `/hud-preview/` and Genesis remain separate blockers and must not dilute Deuteronomy.

## Agent 6 Requested Verdict

Requested verdict: pass, warn-accept, or block this exact text only.

If warned or blocked, please specify exact required wording changes.

## What Must Not Be Accepted

- direct Agent 8 worker routing before Agent 6 signs exact text and Agent 7 publishes the signed boundary;
- Agent 8 as QA authority;
- Agent 8 as worker-completion authority;
- Agent 8 as SOP-law authority;
- Agent 8 as control-state authority;
- Agent 8 as blocker-disposition authority;
- Agent 8 as publication authority;
- Agent 8 as source/provenance authority;
- Agent 8 as public/runtime authority;
- Agent 8 as product/data authority;
- Agent 8 as route publication authority;
- Agent 8 as Definition authority;
- Agent 8 as usage-as-definition authority;
- Agent 8 as accepted-text authority;
- Agent 5 as Agent 8 mandatory general relayer after direct delivery is signed;
- Agent 5 queue/control hygiene as acceptance;
- Agent 12 advice as execution control, blocker closure, or acceptance;
- prompt readiness as worker delivery proof;
- worker output as Agent 6 acceptance;
- active-worker interruption without explicit authorization;
- `AGENT6_REQUIRED` downconverted to `STATUS_ONLY`, `REJECTED_WASTE`, delay, or silence;
- publication readiness;
- source/provenance acceptance;
- public/runtime acceptance;
- deployed/CDN/cache closure;
- product/data gate acceptance;
- route publication support;
- Definition authority;
- usage-as-definition authority;
- translation output;
- accepted translation text.

Publication remains `blocked_no_render`.
