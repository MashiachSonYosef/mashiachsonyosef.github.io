# Agent 10 Orot Pipeline Finish Directive - 2026-06-03

## Scope

Release-owner direction for finishing Orot safely with pipeline data only.

This directive does not claim QA acceptance, validated public/runtime acceptance, source/provenance acceptance, publication readiness, Definition authority, route publication support, usage-as-definition authority, accepted text, or translation output.

## Current Public State

Live URL: `https://mashiachsonyosef.github.io/orot/`

Current live package: Orot top-250, max 25 cards per route key, generated by `scripts/build_public_hud_route_package.mjs`.

- Route package commit: `3719e632f`
- Live proof commit: `baec07c8e`
- Selected token count: `250`
- Public route key count: `342`
- Shard count: `252`
- Card count: `7565`
- Total shard bytes: `20264355`
- Max shard bytes: `375596`
- Inline hints visible before click: `39980`
- Old-HUD marker hits in bounded live proof: `0`
- Poisoned-storage selected glosses in bounded live proof: `0`

Machine proof:

- `reports/agent10-orot-stage-d-top250-cap25-route-package-proof-2026-06-03.json`
- `reports/agent10-orot-stage-d-top250-cap25-live-browser-proof-2026-06-03.json`
- `reports/agent10-orot-stage-d-top250-cap25-live-browser-proof-2026-06-03.png`

## Finish Rule

Orot may expand only by this gate:

1. Generate package from existing route/lexical pipeline data.
2. Keep the Agent 1 source denylist active unless a validator-backed clearance packet removes an entry.
3. Run static output scans for denied IDs, `source metadata incomplete`, and old-HUD markers.
4. Run page/runtime validators.
5. Run bounded browser proof locally.
6. Deploy the smallest passing package.
7. Confirm live manifest counts match the package report.
8. Run bounded live browser proof.
9. Route evidence to Agent 6/4 only after the packet is proof-ready.

No manual semantic edits, accepted translations, source custody shortcuts, or old HUD paths are allowed.

## Agent 1 Direction

Produce one pipeline-only unblock packet for the four denied source blockers:

- `lex-aph-h639`
- `lex-mashiach-h4899`
- `lex-ruach-h7307`
- `lex-yhwh-h3068`

Required output:

- Machine-readable evidence naming each blocked lexicon entry.
- Source/license/citation rows for any entry proposed for clearance.
- Validator proving that clearance is data-derived and does not rely on manual acceptance.
- Updated denylist recommendation: keep denied, clear, or split by source row.

Until this packet passes validation, Agent 10 must keep these entries quarantined from public Route HUD output.

## Agent 2 Direction

Run a cap sweep using only `scripts/build_public_hud_route_package.mjs` in dry-run mode.

Recommended sweep:

- `--top-n 500 --max-cards-per-key 25 --replace-existing --dry-run`
- `--top-n 500 --max-cards-per-key 20 --replace-existing --dry-run`
- `--top-n 1000 --max-cards-per-key 20 --replace-existing --dry-run`
- `--top-n 1000 --max-cards-per-key 15 --replace-existing --dry-run`

Required output per candidate:

- Selected token count.
- Selected lookup candidate count.
- Public route key count.
- Shard count.
- Card count.
- Total shard bytes.
- Max shard bytes.
- Truncated key count.
- Denylist scan total.
- Old-HUD marker scan total.
- Missing/no-route-card counts.
- Whether all currently live top-250 route keys are retained.

Agent 2 must not select reader-facing truth. The output is only feasibility data for Agent 10's release gate.

## Agent 4 Direction

When Agent 10 selects the next bounded package, run independent browser proof with the same live proof shape:

- Cache-busted live Orot URL returns current page.
- Inline hints are visible before click.
- Declared bounded packaged tokens open fullscreen current Route HUD.
- Route cards are visible.
- Source/license/citation rows are visible.
- Route manifest and shards load from `/data/public-hud/orot/route-lookup/**`.
- Old-HUD markers are absent from page text, HUD text, runtime HTML, and old-looking query probes.
- Hard refresh remains current-HUD/no-old-HUD.
- Poisoned localStorage/IndexedDB does not resurrect accepted-translation wording.
- Screenshot and machine JSON are recorded.

Agent 4 must not claim QA acceptance or validated public/runtime acceptance.

## Agent 10 Next Candidate Rule

Agent 10 should select the next package only after Agent 2 cap-sweep data exists.

Preferred next package if gates pass:

- Top-500 route package with the largest card cap that stays below the established byte/shard warning lines and preserves all live top-250 keys.

If no top-500 package passes the static and local proof gates, keep top-250 live and record an exact blocker.

## Agent 6 Review Trigger

Wake Agent 6 only when a packet includes:

- Build report.
- Static validator output.
- Local browser proof.
- Live manifest proof.
- Live browser proof.
- Remaining limits and non-acceptance boundary.

Agent 6 remains the review authority for acceptance wording. Agent 10 supplies evidence only.

## Blockers

Full Orot completion is not safely claimable yet because:

- Full click-time Route HUD coverage has not been generated and proofed.
- Four source blockers remain quarantined pending Agent 1 pipeline clearance.
- The safe cap for package sizes above top-250 is not known until Agent 2 runs the dry-run sweep.

## Agent 8 Callback

Status: `orot_pipeline_finish_directive_recorded`

Artifact path: `reports/agent10-orot-pipeline-finish-directive-2026-06-03.md`

Current public package: Orot top-250 cap-25 live proof passed.

Agent 1 needed: yes, for source-blocker clearance evidence only.

Agent 2 needed: yes, for cap-sweep feasibility data only.

Agent 4 needed: yes, after Agent 10 selects the next package and local proof exists.

Agent 7/13 decision needed: not now; only needed if cap sweep shows no safe expansion path or if full coverage requires a policy choice.

Next recommended executable route: Agent 2 cap sweep, then Agent 10 selects top-500-or-blocker.
