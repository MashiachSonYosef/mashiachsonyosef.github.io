# Agent 7 Agent 8 / Agent 12 Peer Throughput Posture

Generated: 2026-06-02T11:30:00Z

Agent 6 boundary docket: `reports/agent6-agent8-agent12-reconciliation-guardrail-2026-06-02.md`

Superseded operating refinement: `reports/agent7-agent8-primary-driver-agent12-advisory-posture-2026-06-02.md` makes Agent 8 the Agent 7-aligned primary prompter / external driver and Agent 12 an advisory waste limiter. The Agent 6 guardrail and non-acceptance boundary below remain preserved.

## Decision

Agent 7 sets the operating target. Agent 8 and Agent 12 are peer pressure functions under that target:

- Agent 8 pushes for more useful throughput.
- Agent 12 caps named waste before tokens are spent.
- Agent 5 receives executable capped packets, not idle status pings.
- Agent 6 remains QA/compliance authority and may not have validation scope narrowed by Agent 8, Agent 12, or Agent 7.

The active posture remains `RESET_WINDOW_MAX_THROUGHPUT` with a peer-reconciliation cadence layer.

Agent 6 WARN-ACCEPTED the QA-boundary guardrail only. This does not set pace, route workers, mutate SOP law, or accept any output. It does bind the non-acceptance ceiling: `AGENT6_REQUIRED` cannot be converted into `STATUS_ONLY`, `REJECTED_WASTE`, delay, or silence; Agent 12 cannot narrow, veto, or reinterpret Agent 6 evidence scope after Agent 6 states what is required; Agent 8 pressure cannot create acceptance; and slower cadence is not blocker closure.

## Throughput Target

Target all four Agents 1-4 lanes in one of these productive states:

- actively working with delivery proof or a recent artifact trail;
- awaiting Agent 6 or user disposition on a queued evidence packet;
- blocked by an exact non-token blocker with owner, target thread/path, and alternate route;
- carrying a ready 8-hour next-lane packet that Agent 5 can deliver without interrupting an active worker.

Agent 5 should keep at least two ready next-lane packets available when fewer than four worker lanes are actively working or awaiting disposition. A board label alone is not work.

## Pressure And Limiter Triggers

Agent 8 may pressure Agent 5 for:

- material delta;
- new blocker;
- underfilled, stale, or no-proof lane;
- concrete next step that can become an 8-hour assignment or Agent 6 packet;
- scheduled digest.

Agent 12 may cap or shrink:

- repeated proof loops without changed state;
- broad scans or broad renders;
- active-worker interruption;
- status-as-investigation;
- acceptance overclaim;
- vague multi-agent churn without a bounded artifact.

Agent 12 must convert overbroad work into the smallest productive artifact when possible. Agent 12 must not convert `AGENT6_REQUIRED`, user-directed, or Agent 7-priority work into `REJECTED_WASTE`, `STATUS_ONLY`, delay, or silence unless it names a hard blocker and a smaller alternate route.

## Cadence

No idle status pings.

- P0 material change, new Agent 6 blocker, delivery blocker, or owner-route decision: immediate capped packet.
- Underfilled/stale/no-proof lane: at most one capped pressure packet per target every 90 minutes until Agent 5 records delivery proof, an awaiting-disposition state, or an exact blocker.
- Routine throughput digest: every 3 hours, only if it includes changed state, blocked lane evidence, or executable next-lane packets.
- Repeated unchanged proof-loop pressure is rejected unless a new hypothesis or deployment/runtime state change is named.

## Tie-Break Rule

When Agent 8 says more and Agent 12 says less:

1. Agent 6 requirements and blockers win over both.
2. User or Agent 7 max-throughput direction wins unless the proposed action is a named waste class or violates Agent 6 boundaries.
3. If the work advances a P0 blocker, delivery route, or underfilled eligible lane with a bounded artifact, Agent 8's pressure proceeds after Agent 12 caps scope.
4. If the work is an unchanged proof loop, broad scan/render, active-worker interruption, status-only investigation, or acceptance overclaim, Agent 12 caps or rejects it and must propose the smallest productive alternative.
5. If the conflict remains ambiguous, route `AGENT7_DECISION_REQUIRED` with one capped packet: trigger, proposed work, cap, stop condition, artifact, and unaccepted scope.

## Agent 5 Packet Contract

When pressure is warranted, Agent 5 should receive a capped executable packet containing:

- trigger and lane;
- evidence of underfill, blocker, material delta, or concrete next step;
- target worker or Agent 6 queue item;
- proposed 8-hour assignment or validation request;
- delivery target/thread and delivery-proof requirement;
- Agent 12 cap or shrink, if any;
- stop condition;
- what must not be accepted.

## Current P0 Overlay

Agent 6 docket `reports/agent6-public-runtime-license-risk-recheck-directive-2026-06-02.md` preserves a P0 public/runtime license-provenance blocker. Agent 5 must prioritize owner-approved Deuteronomy deploy/swap or non-public quarantine delivery over more local proof. Deuteronomy remains first. `/hud-preview/` remains separate unless the owner-selected route intentionally includes broader public-surface quarantine/deploy.

This P0 overlay does not justify interrupting Agents 1-3 or pulling Agent 4 into another pre-swap proof loop. Agent 4 is useful after live deployed artifacts change and Agent 6 requests runtime/click/source-license validation.

## Boundary

Strategy/cost/throughput control only. No QA acceptance, SOP clean pass, live public/runtime acceptance, old-HUD public use, deployed/CDN/cache closure, source/provenance custody, publication readiness, route publication support, Definition authority, usage-as-definition authority, Reader Workbench broad rollout, product/data gate acceptance, or accepted translation text.

Publication remains `blocked_no_render`.
