# Agent 13 Orot UFM Readiness

Date: 2026-06-04
Status: readiness note only; no package, source, definition, route, runtime, or public mutation

## Working Definition

Until the owner gives a different expansion, UFM means Orot User-Facing Matrix: the smallest product-facing table that tells release work which Hebrew rows are visible to a reader, which English display value they get, why that value is provisional, and what blocker prevents stronger status.

## Current Package Basis

- Package: `data/build/orot/reader-hint-placeholder-candidates.json`
- Current validated path: `127` rows / `4389` occurrences
- Commercial-clean placeholder rows: `97` rows / `4001` occurrences
- Noncommercial educational placeholder rows: `17` rows / `259` occurrences
- `TBD` display-integrity rows: `13` rows / `129` occurrences
- Answer rows: `0`
- Public HUD rows: `0`
- Route JSONL rows: `0`
- Definition-content rows: `0`

The reported `325`-row Spark/pipeline claim is not current package truth. Current Agent 10 plus Spark-2 reconciliation closed the `325` route as stale/mismatched, and Agent 13 reconciliation is recorded in `reports/agent13-orot-325-claim-reconciliation-2026-06-04.md`. The prior `113` row / `4239` occurrence anchor is now pre-append history after Agent 6 cleared and Agent 10 appended `14` rows / `150` occurrences.

## Label Policy In Force

- `counterpart candidate`
- `project-preferred counterpart candidate`
- `TBD` / pending-review placeholder as display separator only

These labels are not definitions, accepted glosses, translations, answers, source acceptance, license acceptance, or publication readiness.

## UFM Start Condition

UFM now runs on the validated `127`-row package basis.

Spark-2 independent verification found no distinct existing-script-produced validated `325`-row package. The `325` route is closed unless a new distinct validated package is produced later.

## First UFM Output

When started, the first UFM artifact should be a compact Orot user-facing row matrix with exactly these columns:

- token id
- Hebrew surface
- occurrence count
- current inline English display
- display status
- source/license lane
- public-emit status
- blocker
- next owner

## Frozen After Spark Reconciliation

- public/runtime mutation
- route-shard edits
- broad dictionary discovery
- Agent 4 runtime proof
- any row becoming answer eligible
- any candidate label being promoted to definition, gloss, translation, or answer

## Agent 8 Callback

The Spark mechanical reconciliation of the `325`-row claim is closed as stale/mismatched. Agent 13 UFM basis is `127` rows / `4389` occurrences until Agent 6 or Agent 1 returns another changed Orot authority boundary.

## Not Accepted

No QA acceptance, source/provenance acceptance, license acceptance, Definition authority, usage-as-definition authority, answer acceptance, public/runtime acceptance, publication readiness, route publication support, product/data acceptance, translation output, accepted gloss, or accepted text.
