# Agent 5 Measurable Everyone Goals - 2026-06-04

Status: corrected measurable production-goal packet for Agent 12 approve/cap after Agent 7/8 current-anchor correction.
Highest permissible claim: measurable goal map prepared and routed for advisory limiter review only.

## Trigger

Oracle 9 returned `REVIEW_CHANGES` against the prior goal map because it still read like role labels rather than production goals.

Durable owner/oracle standard: `reports/oracle9-actual-goal-standard-and-2000-work-corpus-direction-2026-06-04.md`.

Agent 7/8 current-anchor correction: `reports/agent7-agent5-production-shaped-goal-map-2026-06-04.md` used the stale pre-append Orot anchor. Current Orot anchor is the post-append package:

- Package: `data/build/orot/reader-hint-placeholder-candidates.json`
- Current rows / occurrences: `127` rows / `4389` occurrences
- Post-append proof: `reports/agent10-orot-14-row-post-append-proof-2026-06-04.md`
- Machine proof: `reports/agent10-orot-14-row-post-append-proof-2026-06-04.json`
- Agent 6 docket: `reports/agent6-orot-14-row-nonpublic-add-candidate-verdict-2026-06-04.md`
- Boundary preserved: zero public/runtime/output/answer/definition/accepted-text emissions.

Historical pre-append anchor only:

- `113` rows / `4239` occurrences
- commercial-clean `83` rows / `3851` occurrences
- NC educational `17` rows / `259` occurrences
- TBD display-integrity `13` rows / `129` occurrences

## Production Goal Standard

Every active lane goal must name:

- scope;
- target count or coverage percentage;
- allowed license ceiling;
- output artifact class;
- validator or gate;
- stop condition;
- blocker list.

Bad goal: `finish Orot`.

Good goal: build the safe-license reader-hint / definition corpus for the target 2000-work corpus using only Agent-6-approved license lanes at the NC-or-safer ceiling, reach 100% match where possible, record every excluded/unmatched row, and make every package validator-backed before public/runtime promotion.

## License Lanes

`commercial_clean`: Public Domain, CC0, CC BY, and other Agent-6-approved commercial-clean sources.

`noncommercial_educational`: CC BY-NC only after Agent 1/6 custody, attribution, storage/display, and noncommercial boundary; `commercial_export_allowed=false`.

`conditional`: BY-SA or complex licenses only if Agent 6/legal pipeline explicitly marks compatible for the target package.

`metadata_or_link_only`: external-link-only, metadata-only, no-derivatives, proprietary, unknown, or uncleared sources.

## Mission Goal

Build safe-license reader-hint / definition coverage for the target 2000-work corpus, Orot first, then expansion. Each package must report:

- work count covered;
- token, lemma, or subphrase rows covered;
- occurrence count covered;
- coverage percent;
- commercial-clean coverage;
- NC educational additional coverage;
- excluded rows by license reason;
- unmatched rows;
- rows awaiting Agent 1;
- rows awaiting Agent 6;
- rows blocked by transform, schema, or linkage;
- rows added after Agent 6 clearance;
- validation commands and results.

Immediate Orot interpretation:

- use the current `127` row / `4389` occurrence safe non-public placeholder/candidate package as the active Orot anchor;
- treat the prior `113` row / `4239` occurrence basis as historical pre-append evidence only;
- preserve the Agent 6 WARN-ACCEPTED exact non-public append boundary for the `14` row / `150` occurrence packet;
- preserve commercial-clean versus NC educational lanes;
- use `TBD` only as display-integrity separator text;
- continue missed-dictionary evidence before broad discovery;
- report remaining gap as exact blocked rows.

## Lane Goals

### Agent 13

Scope: mission selection, priority, and resource allocation for the 2000-work safe-license corpus target.

Target: keep the mission measurable as work-count, row-count, occurrence-count, coverage percent, remaining gap, and public-readiness blocker.

License ceiling: policy only; no license acceptance.

Output artifact class: owner/mission decision packets naming target scope, exception policy, and priority order.

Gate: Agent 6 remains QA/compliance authority for acceptance boundaries.

Stop condition: stop at mission/resource decision; do not manage routine worker execution.

Blockers: unclear target size, policy exception request, budget freeze, or conflict between Orot-first and broader corpus sequencing.

### Agent 7

Scope: execution management for measurable lane goals and staffing.

Target: every represented active lane has one measurable production goal and one stop condition; no status-only goals.

License ceiling: preserve lane license ceilings; no license acceptance.

Output artifact class: execution decisions, staffing/wake decisions, delivery-proof records, and goal corrections.

Gate: Agent 6 for QA; Agent 12 advisory cap for waste; Agent 13 for mission/resource change.

Stop condition: stop after a blocker resolution, staffing decision, or goal correction.

Blockers: vague lane goal, duplicate fanout, stale target, missing delivery proof, or underfilled lane without a bounded task.

### Agent 10

Scope: release/package sequence owner for Orot first, then expansion toward the 2000-work target.

Target: produce package sequence plans that quantify current package rows, occurrences, work count, license-lane split, excluded/unmatched rows, and next package target.

License ceiling: NC-or-safer only unless Agent 6/legal pipeline explicitly clears a higher-risk conditional lane; `commercial_export_allowed=false` for NC-derived rows.

Output artifact class: release/package direction packet, Agent6-ready package packet, coverage matrix, or exact package blocker.

Gate: Agent 6 for source/license/display/runtime/package authority; Agent 1 for source/license maps; Agent 2 for transforms; Agent 4 for runtime proof after changed package.

Stop condition: stop after selecting next package, routing Agent6-ready packet, or naming missing input/blocker.

Blockers: missing row-level source/license map, missing transform artifact, missing Agent 6 boundary, package validator failure, unclear commercial/NC split, or runtime proof needed after changed package.

### Agent 1

Scope: broad row-level source/license/custody/manifest packaging for candidate corpus packages, with Orot maps current high-priority when release sequencing depends on them.

Target: for each package candidate, classify 100% of included rows as `allow`, `exclude`, or `block`, with attribution/storage/display requirement and license lane.

License ceiling: commercial-clean and noncommercial-educational only unless Agent 6/legal pipeline explicitly marks conditional sources compatible.

Output artifact class: row-level source/license/custody map JSON and MD, manifest blocker map, attribution requirement map, or exact source/linkage blocker.

Gate: Agent 6 source/license/custody boundary review before use as clearance.

Stop condition: stop after row map covers the package candidate or names exact missing source/linkage/manifest evidence.

Blockers: missing lexicon/source linkage, unknown/proprietary/ND source, missing attribution data, missing manifest, custody conflict, or Agent 6 boundary required.

### Agent 2 / Definer-2

Scope: broad lexical/definition/reader-hint/transform packaging using existing pipelines, with exact Orot transform/blocker as current high-priority.

Target: emit deterministic non-public reader-hint/definition packages that cover 100% of safe rows in the selected package where possible, with every excluded/unmatched row recorded.

License ceiling: commercial-clean plus Agent-1/6-cleared NC educational rows; NC rows must carry `commercial_export_allowed=false`.

Output artifact class: JSONL transform package, reader-hint package, transform safety matrix, schema-fill output, or exact transform blocker.

Gate: package validator, route/answer safety validator, Agent 1 source/license map, Agent 6 boundary review before any acceptance or public/runtime promotion.

Stop condition: stop after candidate artifact, zero-safe no-output result with blocker inventory, or exact missing schema/linkage/license blocker.

Blockers: manual definition required, ambiguous candidate, missing answer-role field, missing source/license join, unsafe NC/Kaikki/grammar promotion, schema mismatch, or validator failure.

### Agent 3

Scope: linkage, dedupe, navigation, and provenance maps that help Agent 1/2/10 raise coverage safely.

Target: for named package inputs, produce 100% linkage/dedupe/navigation accounting over the package rows and report duplicates, unmatched references, route joins, and provenance paths.

License ceiling: metadata/linkage evidence only; no source/license acceptance.

Output artifact class: linkage map, dedupe report, provenance navigation map, route/usage join check, missed-dictionary diff, or continuation index.

Gate: Agent 2 owns definition/transform output; Agent 1 owns source/license maps; Agent 6 owns acceptance boundaries.

Stop condition: stop after named input is fully mapped or exact missing input/pipeline blocker is recorded.

Blockers: missing input artifact, missing route ID, duplicate unresolved rows, ambiguous join, missing provenance pointer, copied payload risk, or no named pipeline.

### Agent 4

Scope: runtime/public proof packages only when changed validated packages require proof, plus validator/prerequisite packaging on exact named inputs.

Target: for each changed package routed by Agent 10 or Agent 6, produce exact runtime/prerequisite proof with 100% coverage of named URLs/files/dependencies in that package scope.

License ceiling: no license acceptance; preserve source/license visibility checks only.

Output artifact class: runtime gate packet, validator result packet, static prerequisite report, file/hash diff report, marker inventory, or exact runtime prerequisite blocker.

Gate: Agent 6 for public/runtime acceptance; Agent 10 for changed package route; no broad proof without changed package or named validation input.

Stop condition: stop after package proof/prerequisites are produced or exact missing package/runtime input is named.

Blockers: no changed package, missing URL/file list, missing build/source-of-truth, old-HUD marker hit, payload threshold breach, validator failure, missing source/license visibility, or cache/runtime ambiguity.

### Agent 5

Scope: queue hygiene, delivery proof, exact blocker preservation, Spark queue dedupe, and Agent 6 packet flow.

Target: every active goal must have measurable scope, count/percent target, license ceiling, artifact class, validator/gate, stop condition, and blocker list before it is treated as durable.

License ceiling: preserve lane ceilings only; no license acceptance.

Output artifact class: coordination packet, delivery-proof record, goal-map correction, Agent6-ready packet, or exact delivery/control blocker.

Gate: Agent 6 for QA; Agent 7 for execution priority; Agent 12 for advisory cap; Agent 13 for mission/resource change.

Stop condition: stop after routing proof, corrected goal packet, Agent6-ready packet, or exact blocker.

Blockers: stale target, unavailable send tool, vague goal, missing metric, missing validator, duplicate fanout, broad proof loop, or acceptance-like wording.

### Agent 6

Scope: QA/compliance boundary review for source/license/display/runtime/package questions.

Target: return exact pass/warn/block or exact blocker on routed evidence packets; do not own production.

License ceiling: decides boundaries by docket; no Agent 5/7/8/10 packet can narrow scope.

Output artifact class: dated docket verdict and machine evidence where applicable.

Gate: source evidence, package evidence, runtime evidence, and non-acceptance boundaries must be exact.

Stop condition: stop after verdict or exact missing-evidence blocker.

Blockers: summary-only evidence, missing source/license row proof, missing runtime source-of-truth, missing validator, ambiguous license lane, or acceptance overclaim.

### Agent 8

Scope: concrete pressure, delivery-proof routing, and immediate authority-route escalation.

Target: pressure only for concrete output, exact blocker, delivery proof, underfilled measurable queue, or Agent 7/10 route request.

License ceiling: no license authority.

Output artifact class: bounded pressure packet, delivery proof, callback receipt, or exact routing blocker.

Gate: if Agent 10 produces an Agent6-ready packet, route immediately; do not wait for Oracle 9, Agent 12, or pulse cadence.

Stop condition: stop after concrete route, delivery proof, or exact blocker.

Blockers: no concrete artifact, duplicate pressure, broad prompt churn, status-only request, stale target, or acceptance-like wording.

### Oracle 9

Scope: outside-owner surveillance, owner-context drift, goal-quality review, and public-vs-accepted drift leads.

Target: flag vague goals, stale claims, missed resource lanes, and public/accepted drift; propose measurable wording when needed.

License ceiling: no license authority.

Output artifact class: owner/oracle review packet, drift note, or decision-packet input.

Gate: Agent 6 for acceptance; Agent 7/13 for priority.

Stop condition: stop after review verdict or exact drift/blocker.

Blockers: role slogans without metrics, stale public claims, missing counts, missing license ceiling, or missed databank/corpus resource.

### Agent 12

Scope: advisory spend/cap review for measurable goals.

Target: approve, cap, route to Agent 6, duplicate/churn flag, or escalate based on scoped work quality.

License ceiling: no license authority.

Output artifact class: advisory limiter approval/cap packet with exact shrink reason when applicable.

Gate: may not suppress Agent 6-required work or convert acceptance questions into cost decisions.

Stop condition: stop after `APPROVE`, `CAP`, `ROUTE_AGENT6`, `DUPLICATE_OR_CHURN`, or `ESCALATE`.

Blockers: broad work, missing target count, missing stop condition, missing artifact class, missing validator/gate, duplicate fanout, or status churn.

### Sparks

Scope: named existing mechanical pipelines only.

Target: extraction, counts, dedupe, diffs, validators, schema fill, continuation records, and mechanical packet prep for named inputs.

License ceiling: no license authority.

Output artifact class: mechanical matrix, count report, validator result, diff, schema-fill output, continuation record, or `missing_pipeline_blocker`.

Gate: outputs go to Agent 10 first when they affect release/package sequencing, then the relevant Agent 1-4 packaging lane.

Stop condition: stop after producing the named mechanical output or `missing_pipeline_blocker`.

Blockers: missing command, missing input file, missing output path, missing schema, missing key/count definition, or request to design/invent pipeline.

## Delivery Status

Agent 7 already delivered broad standing packaging goals to Agents 1-4 in `reports/agent7-agent5-goal-reactivation-response-2026-06-04.md`.

Agent 5 delivered Definer-2 Orot high-priority assignment:

- Target: `019e027b-7533-7272-9474-7abaf8712b29`
- Submission: `019e9243-4413-71a2-af45-2058ae3a7db4`

Earlier Agent 12 approval of this measurable packet is superseded for anchor accuracy by the Agent 7/8 correction above. This corrected packet is the active version for Agent 12 approve/cap review.

## Boundary

This is production-goal definition and delivery-hygiene only. It creates no QA acceptance, source/provenance acceptance, license acceptance, Definition authority, usage-as-definition authority, answer acceptance, public/runtime acceptance, publication readiness, route publication support, product/data acceptance, translation output, accepted gloss, or accepted text. Publication remains `blocked_no_render`.
