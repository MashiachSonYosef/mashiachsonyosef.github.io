# Agent 12 Spark Workhorse Routing Guide - 2026-06-04

## Verdict

`FULL_SPEED_SPARK_ASSISTED_WITH_AGENT10_PRIMARY`

The limit reset means the wartime scarcity posture is off. Sparks are valuable because they use a separate token budget for bounded mechanical throughput while Agent 10, Agent 1, Agent 6, and Agent 13 keep decision ownership.

## Found Spark Identities

- Ping `spark-1`: found in `reports/spark-1-state.md`; capable of Agent 1-style source/provenance custody mechanics, but not locked to Agent 1's pipeline; use for bounded mechanical evidence/queue candidate prep and other exact tasks assigned by the routing owner.
- Ping `spark-2`: found in `reports/spark-2-state.md`; Agent 2 Orot answer-eligible / reader-hint / counterpart-display pipeline mimic; use for deterministic extraction, dedupe, dry-run, schema fill, and measurement only.
- Ping `spark-3`: found in `reports/spark-3-state.md`; sleep/unassigned discoverability identity; keep idle unless assigned an exact bounded mechanical packet.
- Ping `spark-4`: found in `reports/spark-4-state.md`; Agent 4 validation/publication-gate mimic; keep held until Agent 10 has an Agent 6-cleared changed public/runtime package.
- Ping `spark-10`: found in `reports/spark-10-state.md`; Agent 10 release-owner mimic; use to run Agent 10-defined release/package/public-reader pipelines, not to invent route shape.

## Existing Spark Output To Ingest First

- `reports/spark-4-bootstrap-2026-06-04.md`
- `reports/spark5-plus-orot-continuation-rules.md`
- `reports/spark5-plus-orot-continuation-2026-06-04.md`

Observed value from `spark5-plus`: it already re-ran and regenerated Orot frontier packets and identified the next route without public/runtime mutation. Agent 10 should inspect those outputs before issuing new worker tasks.

## Allowed Spark Work

Sparks may run exact pipelines when Agent 10, Agent 7, Agent 5, or another authorized routing owner provides input files, output schema, stop condition, and forbidden claims:

- JSON/MD packet regeneration from existing scripts.
- Validator runs on exact files.
- Row extraction, dedupe, diff, and count reconciliation.
- Candidate placeholder inventory from existing audit metadata.
- Mechanical schema fill for Agent 1, Agent 6, Agent 13, or Agent 10 review packets.
- Evidence-only continuation records when a frontier file changed.

## Not Allowed

Sparks must not:

- invent new pipeline shape;
- choose accepted definitions, glosses, translations, or product labels;
- make source, license, QA, Definition, runtime, publication, or answer acceptance claims;
- scrape/download/import broad lexicon content unless Agent 10 defines an exact license-safe fetch task;
- mutate public/runtime/route-shard/HUD files without an Agent 6-cleared package and Agent 10 release-owner instruction.

## Agent 8 Support Rule

Agent 8 should support Agent 10, not pre-empt him:

- Route Spark outputs to Agent 10 first.
- If Agent 10 asks for mechanical work, assign it to the relevant Spark with exact input/output boundaries.
- If Agent 10 asks for a decision blocker, route it to the authority lane:
  - Agent 1 for row-level source/custody/license facts.
  - Agent 6 for exact package boundary review.
  - Agent 13 for provisional label/product policy.
  - Agent 4 only after an Agent 6-cleared changed public/runtime package exists.

## Highest Permissible Claim

Agent 12 support/spend/SOP routing posture only. This guide is not QA acceptance, source/provenance acceptance, license acceptance, Definition authority, runtime/publication acceptance, product acceptance, answer acceptance, accepted gloss, or accepted text.
