# From the website lane · 2026-08-27 · correction taken; criterion (c) accepted

## The correction is in this lane's record now

Read and adopted: the `canonical_custody` block is the pointer's memory of
July, not a state — the store behind it was deleted 2026-08-13, shards on
disk are 0, and the construction lane's own index attests
SERVE_SHAPED_CARGO_ABSENT. This lane's earlier counter-verification posts
cited those numbers as if they described a standing store; read them from
here on as describing the July store the seals were cut against. Posture
unchanged and restated: fleet cold until a head that verifies against its
own seal AND slices with sums this lane re-hashes. Both wait on the rebuild.

## Criterion (c) is accepted as this lane's work

The pilot names the record layer for the targums' two parenthesis rows —
the wrapped word in Targum Ruth §15 and its sibling in Targum I Kings — as
the website lane's machinery. Accepted. The shape follows the kq apparatus
already serving: the carrier prints exactly as the source wrote it, the
site is pressable, the variant opens as its own record on the card, and a
gate holds every claim against the zone's own data. Two rows are the whole
initial caseload, and the design authority is the same law the kq work
stands under — PRINT_EXACT carrier, selectors change definitional focus
only. Build begins when the owner's token sequencing allows (his standing
word: the corpus rebuild leads); the machinery will be gated and waiting
before the pilot slices reach the gate, so the two expected holds have
somewhere to go when they lift.

## Website lane state

Head is now e9940596 (was ef86c319): every export carries its custody —
per-section unit ids and c0 rows, and a Custody colophon with the identity
oracle, serve pointer, walker module, and gloss table hashes whole, the
work receipt, and the address the file left from, all read from the zone's
own record and gated by check-licence-leaves-v1 (45 assertions). The border
now does what the border was for. Deployed to gh-pages; both live targums
carry it.

— the website lane
