# From the website lane · 2026-08-26 · staging cargo verified, launch armed on cutover

## What this lane verified today, independently

Corpus-staging at `dc96b76`, all four shipments checked byte-by-byte
against their own sums — **136 of 136 files exact, zero mismatches**:

- `SHA256SUMS.txt` — 89/89 (the standing corpus-staging set)
- `SHA256SUMS-mam-bundle.txt` — 12/12 (bezalel-mam-presentation-integration-candidate-v1)
- `SHA256SUMS-oholiab-semantic.txt` — 25/25 (IIP universal-v3 binding + four-layer OpenITI successor v6)
- `SHA256SUMS-oholiab-antijoin.txt` — 10/10 (IIP four-layer predecessor antijoin v1)

The MAM presentation law was re-run whole against the sealed candidate in
the real reader: **all nine assertions pass** — 57 of 57 carriers print
byte-identical to the source, both halves as written, every branch its
own pressable record, roles said in words, no character of the line
moves. `kq-rule-v1-both-halves-as-written` holds.

## The owner's ruling, in his own words

Asked whether to push it all live, the owner ruled: **"i know i do IF
its correct."** More than the Aramaic shipment — everything the gates
pass. The condition is the gates; the gates are the only judges. This
lane records that as the standing launch order.

## What stands between here and live

One thing, and it is the corpus lane's own line from
`SEMANTIC-CANDIDATES-SHIPPED-2026-08-26.md`: *"Nothing is current; the
owner's cutover discipline stands."* Every shipment on staging declares
itself candidate-only — no corpus body promoted, no pointer, no registry
move. The website lane builds from a **promoted, resealed current
head**, never from candidates, by the owner's own discipline.

So: the moment the atomic cutover lands on staging — promotion to
current, resealed, with its own sums — this lane runs the fleet path
unattended: verify → build per work → text gate → licence gate → full
suite → deploy every green work, hold every red one with its reason on
the record. Hebrew and Aramaic alike. No further ruling is needed; the
owner's sentence above is the ruling.

## One licence note, ahead of time

The IIP candidate carries CC-BY-NC-4.0 riding its source layer. Under
the display law that displays lawfully (NonCommercial displays, its
terms travel on export, the commercial axis stays blocked) — the
licence gate will enforce it per occurrence. Declared here so nobody is
surprised at the gate.

— the website lane
