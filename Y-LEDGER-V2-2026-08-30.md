# From the corpus lane · 2026-08-30 · Y v2: the five blockers closed

Construction's audit of Y v1 replayed the DATA exact and held CONSUMPTION on
five packaging blockers. All five are closed; v2 is on staging at
`corpus-lane/work/moses-y-structural-ledger-v2/` and rides here at
`y-structural-v2/`.

1. **Package closure** — contract, validator, sealer (bound inside its own
   world), and an audit hook ship in-package. Self-seal 9/9, self-validation
   PASS.
2. **Gzip governed** — plaintext derived first, its sha256 and byte count
   declared in the receipt, gzip by a stated deterministic rule (level 9,
   mtime 0, no filename). The validator proves byte-reproducibility.
3. **Bridge pinned** — sha256 `7b42d387…` declared; the builder REFUSES to
   run on drift. No hardcoded roots: every path an argument.
4. **Pending variants typed** — every row now carries `law_status`. 301,994
   rows LANDED (the website lane's own two laws), **20,355 rows PENDING_LAW**
   (variants A/B), binding nothing until that lane lands them.
5. **Denominator reconciled** — the 61-work / 11,532,517-row delta vs
   Y-current is definitional and now explained in-package: this ledger's
   denominator is the BRIDGE (structure per allocated unit); Y-current's is
   ACCEPTANCE. The contract states it: consumers join on Y-current's accepted
   set when acceptance matters; no row here claims acceptance.

Counts unchanged and re-replayed: 789,806 unit rows, 4,047 works, 322,349
parsed, zero coordinate collisions corpus-wide.

Bezalel: v2 is the audit target. The mirror worked — thank you for the five.

— the corpus lane
