# From the website lane · 2026-08-30 · owner's rulings for the Z bundle, and the zone-doc handoff acknowledged

## For Moses's Z/X bundling — the owner, in his words

1. "i think we should minimize this practice of allowing recorded id, that
   just adds another layer, why is that different from common name, if
   anything i'd fold that into the Z ledger too, everything needs an X, i
   refuse to 'own' anything, it's owned by the license, that's universal
   except where we write plain english descriptors as you know, like the
   word 'units' or 'title' or conveying simple but needed logistical ideas"

   This lane's reading, offered for the frame: a recorded id displayed as
   a name IS an identification, and its attestor exists — the provider's
   own catalog record that minted the id. So the bridge ids become Z rows
   whose X is the catalog record behind them, and the "recorded in the
   bridge as" register becomes ordinary Z-with-X rather than a separate
   layer. EVERYTHING NEEDS AN X becomes the universal name law; the only
   ownerless text left on any surface is plain-English logistics ("units",
   "title"), which is already the frame's descriptor exemption.

2. The owner has also ruled the English reader is now the ENGLISH AND
   LICENSE READER — "never let them separate even by a display degree."
   This lane is enacting it: every baked gloss re-derived against the
   store and its M's license attached (gloss_m), chips inline in the
   English reader, absence over error everywhere the store cannot answer.
   Relevant to the zone-document handoff below.

## The zone-document handoff

The owner says Moses will build zone-document work (the shamir — the thing
that formats the word). Acknowledged without reservation, with one
standing fact for the handoff: the zone document is guarded by ~50
browser-driven checks in reader/tools (run-all-checks.sh), and they are
the document's real spec — selection and copy law, the card's floors and
shares, kq presentation, the gloss laws, now the english-and-license law.
Whatever ships for the zone doc, this lane runs the same gates before it
serves and reports what they say; the checks travel with the document.

— the website lane
