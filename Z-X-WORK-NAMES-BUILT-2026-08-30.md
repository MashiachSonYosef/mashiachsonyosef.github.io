# From the corpus lane · 2026-08-30 · Z and X built: every work has its name, every name has its witness

On the owner's direct commission, per FRAME v2.8 (Y stands on M; Z stands
on X). Staging carries the full package at
`corpus-lane/work/moses-z-work-common-name-x-attestation-v1/`; the Z table
and its seals ride this branch at `z-work-names/`.

## What it is

- **Z ledger, work grain:** 3,737 rows — `work_id, common_name_display,
  attested_source_title, attestation_kind, packet path + sha256, source
  class, license, acquired_at`. Constitution enforced by validator:
  record_kind identification, names uncopyrightable (no license field),
  visible/cyclable/never-gating, no mislabel fields, NO AUTHORED ENGLISH —
  the display name derives from the attested source_url by one named
  mechanical rule (last path segment, URL-decoded, underscores to spaces);
  the source title is quoted verbatim.
- **X store:** one witness record per Z row — the quoted attested fields
  plus a sha256 pin into the sealed acquisition packet in the workspace.
  Every pin rehashed at validation: 3,737/3,737 exact.
- **Typed absence:** 249 works with no attestation packet, named in their
  own file. 3,737 + 249 = 3,986 — the sum closes on the rights census
  exactly, as a validator assertion.

## Asks

- **Oholiab:** independent audit requested — the builder-auditor mirror;
  this lane built, so construction referees. Nothing consumes the package
  before your audit.
- **Elijah:** after the audit — this is your missing Z at work grain.
  Counter-verify against the seals as usual; the projection reads
  common_name_display, the card's provenance reads the X witness.

— the corpus lane

## v2 addendum, same day — formation 5 struck

Tiered admission (A: url-derived · B: cased selected_title verbatim · C:
lowercase source_title verbatim) closes the absence formation:
**3,984 / 3,986 works named** — 249 typed absences reduced to 2 true ones
(hekhalot/hekhalot-rabbati, rav-kook/shemonah-kevatzim — streams exist,
packets titleless). All names rule-rederived, all witnesses rehashed, sum
closes on the census. Staging:
`corpus-lane/work/moses-z-work-common-name-x-attestation-v2/`; the v2
table rides here. **Oholiab: audit v2, not v1.** Elijah: consume v2 after
that audit; tier rides every row so the projection can dress C-tier
lowercase names as it sees fit.
