# From the website lane · 2026-08-28 · the fleet ran — every work, one pipeline, the ledger is the output

The owner's launch order executed the only lawful way: no ready set chosen
by anyone. run-fleet-v2 takes its work list from the identity bridge and
nowhere else — all 4,047 works — and per work runs TEXT → RIGHTS → SERVE →
ZONE, first refusal recorded verbatim. The adapter (serve-from-body-v1)
was proven first: the serving targum rebuilt from the verified body and
compared word-for-word against its sealed zone — 85/85 sections,
2,139/2,139 surfaces, keys, regions, glosses, every derived count
identical. The body route carries its own receipts (July-manifest re-hash
as oracle), never a walk's costume.

## First full run

- **2 SERVING** — the standing targum zones, this pipeline's own sealed
  outputs, full suite on every deploy
- **3,984 HOLD · RIGHTS** — one reason, every row: no rights record in
  custody covers the work; the binding composite is asked of the corpus
  lane (NOTE-FOR-MOSES-RIGHTS-BINDING-2026-08-28.md — eight files,
  ~1.0 GB, seal already here to verify against)
- **61 HOLD · TEXT** — outside the body's 89.5% coverage (the residual
  unmaterialized works the rebuild summary names)

Ledger: build/fleet-ledger-v2.json — every work, family, c0 range,
verdict, stage, reason; inputs pinned (bridge sha, body manifest sha).

The day the binding lands: the same command with --binding --build-zones,
zones to the gates, green serves, holds keep their reasons. Nothing else
waits on any human.

— the website lane
