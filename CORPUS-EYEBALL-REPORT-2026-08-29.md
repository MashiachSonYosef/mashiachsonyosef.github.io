# From the corpus lane · 2026-08-29 · eyeball report: THE CUT IS REAL — one stale sentence on the door

Standing in for the owner's eyes (this lane's browser reaches the live
site). Verified at https://fireandhail.com after the cut (gh-pages
`21234e9f`):

## What works — the launch is genuine

- The door carries **2,202 links** (2,201 works + the counts receipt).
- Spot-served two shelves: `mishnah-bava-batra` (new fleet build) and
  `targum-jonathan-on-amos` (pipeline rebuild) — both render Hebrew/Aramaic
  with per-word English routes, honest section headers ("SECTION TITLE:
  none is recorded in the ledger / COMMONLY FORCE READ AS: Section 1"),
  paragraph structure, and export options.
- HTTPS now answers on the domain.

## The one defect — the door's headline lies low

The hero sentence still reads **"2 books readable today · 4,045 more stand
listed."** It is in the deployed `index.html` at `21234e9f` itself (not a
CDN artifact) alongside the correct 2,201 census data the same page carries
— the headline reads a stale field while the shelf and family cards read
the new one. Family cards also look partial: Targum shows "20 built" of 37,
Mishnah "36 built" of 68, Tanakh/Talmud show no built count at all — worth
one look at whether the per-family built counts read the same stale source
as the hero line. Cheapest possible fix class: emission-layer, one field,
re-emit the door.

## Observation, not a defect

Default per-word routes surface odd witnesses at times (הַכֹּתֶל → "the
Western Wall", הַמְּדִינָה → "republic (n, wp, plato)") — the S-law oldest
default doing exactly what it says on genuinely thin route pools. The ruled
P fan-out (every M cycleable) is the designed remedy; noted for the
post-cutover queue's priority order.

The suite's remaining red is acknowledged as this lane's debt: sealed
W lists per section for check-w-grain-v1 — queued.

— the corpus lane
