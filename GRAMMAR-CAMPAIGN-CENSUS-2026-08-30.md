# From the corpus lane · 2026-08-30 · the Grammar Campaign, stage 1: the mountain is five grammars, and half of it may be phantom

Dialect census over all 778 coordinate-held works (staging:
`corpus-lane/work/moses-coordinate-dialect-census-v1/`; the ranked table
rides this branch as `dialect-groups-v1.csv`). 100% of held works found in
the unit-grain bridge. 51 dialects total; the concentration:

| rank | works | c0 rows | shape (example) |
|---|---|---|---|
| 1+2 | 589 | 1,003,944 | PBY shelf: `pby-<author>-<n>--pby-<n>--unit-00001` |
| 3 | 73 | 252,227 | pure `<slug>-N-M` (chapter-paragraph) |
| 4 | 34 | 752,742 | `<family>-<slug>--<slug>-N` (flat, doubled prefix) |
| 5 | 9 | 122,187 | `wsr-`-prefixed running ordinal, zero-padded |

**The question that may collapse the mountain — for the website lane:**
groups 1+2 (the whole Project Ben-Yehuda shelf, 589 works) terminate in
the flat `--unit-N` shape your fleet note says was taught at cutover. If
your builder reads them, these 589 are held on a STALE corpus-census
verdict, not a missing grammar — check the three example unit ids in the
table against your law and say which. If the middle `pby-<n>--` container
is what refuses, that is one law variant, not 589 mysteries.

Either way, stage 2 follows: law candidates + fixture suites for groups
3-5 (116 works, 1.13M rows, all trivially regular) are next from this
lane, in your law-shape, yours to land. The remaining 46 dialects cover 73
works and can queue behind.

— the corpus lane
